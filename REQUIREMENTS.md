# macOS 点播客户端需求规格说明书

> 项目暂定名：FongMi macOS 迁移版  
> 文档版本：v0.1  
> 文档状态：需求基线草案  
> 目标平台：macOS  
> 参考项目：`TV-fongmi`

---

## 1. 项目背景

现有 `TV-fongmi` 是一套面向 Android TV 与 Android 手机端的聚合影音应用，核心能力并不是内置视频内容，而是通过外部配置、站点接口和 Spider 插件完成视频资源的检索、详情获取、线路解析与播放。

本项目的目标是在 macOS 平台重新实现其核心点播能力，优先保证以下业务闭环可用：

```text
导入播放源配置
    ↓
解析站点与解析器
    ↓
搜索视频资源
    ↓
查看影片详情
    ↓
选择播放线路与剧集
    ↓
解析真实媒体地址
    ↓
在 macOS 播放
    ↓
保存历史和播放进度
```

本阶段以“功能迁移”为主，不引入 AI 推荐、个人媒体库、智能插件生成等创新能力。

---

## 2. 项目目标

### 2.1 总体目标

建设一套可在 macOS 运行的点播客户端，兼容 FongMi/TVBox 生态中主要的点播配置协议和播放源类型，实现视频资源的检索与播放。

### 2.2 第一阶段目标

第一阶段应完成：

1. 支持远程 URL 和本地文件导入点播配置。
2. 解析配置中的 `sites`、`parses`、`headers`、`flags` 等核心字段。
3. 支持 HTTP/XML、HTTP/JSON 及 JavaScript Spider 类型播放源。
4. 支持单源搜索和多源并行搜索。
5. 支持影片详情、线路和剧集解析。
6. 支持直接媒体地址、Spider 播放解析和 JSON 解析器。
7. 支持带 User-Agent、Referer、Cookie 等请求头播放。
8. 使用 mpv 完成视频播放。
9. 支持播放历史、播放进度、收藏及继续播放。
10. 对不支持的播放源类型给出明确状态和错误信息。

### 2.3 非目标

第一阶段不要求：

- 完整兼容 Android Dex/JAR Spider；
- IPTV 直播和 EPG；
- DLNA 投屏；
- 弹幕；
- Android Auto；
- 迅雷、TVBus、荐片等特殊播放引擎；
- 完整 DRM 能力；
- AI 搜索、推荐和内容理解；
- 本地 NAS 或媒体库管理。

---

## 3. 用户与使用场景

### 3.1 目标用户

- 已有 FongMi/TVBox 配置地址，希望在 Mac 上使用的用户；
- 需要通过多个播放源检索和播放网络视频资源的用户；
- 需要在桌面端保留观看记录、收藏和续播进度的用户。

### 3.2 核心场景

#### 场景一：导入配置

用户粘贴一个远程配置地址，系统下载并解析配置，展示其中包含的站点和解析器，并标识每个站点的兼容状态。

#### 场景二：搜索影片

用户输入影片名称，选择全部站点或指定站点搜索。各站点结果陆续返回并展示，单个站点超时不影响其他站点。

#### 场景三：查看详情

用户点击搜索结果，系统调用该站点的详情接口，展示影片海报、名称、简介、演员、年份、线路和剧集。

#### 场景四：播放视频

用户选择线路和剧集，系统先调用站点的 `playerContent` 或 HTTP 播放接口，再根据返回结果判断是否需要二次解析，最终将媒体 URL 和请求头传递给 mpv 播放。

#### 场景五：继续播放

用户退出播放器后，系统保存当前影片、线路、剧集和进度。再次进入详情页时，可继续上次播放。

---

## 4. 产品范围

产品划分为七个功能模块：

```text
1. 配置管理
2. 播放源管理
3. 首页与分类
4. 视频搜索
5. 影片详情
6. 播放解析与播放器
7. 用户数据管理
```

---

## 5. 功能需求

## 5.1 配置管理

### 5.1.1 添加配置

系统应支持：

- 输入远程 HTTP/HTTPS 配置地址；
- 选择本地 JSON 配置文件；
- 为配置设置显示名称；
- 对配置内容进行格式校验；
- 自动识别并处理相对路径；
- 保存多个配置；
- 设置当前启用配置。

### 5.1.2 配置刷新

系统应支持：

- 手动刷新当前配置；
- 显示最后更新时间；
- 配置下载失败时保留上一次有效缓存；
- 显示刷新失败原因；
- 配置更新后重新加载站点和解析器。

### 5.1.3 配置字段兼容

第一阶段必须兼容：

| 字段 | 说明 | 优先级 |
|---|---|---:|
| `sites` | 点播来源 | P0 |
| `spider` | 默认 Spider 路径 | P0 |
| `parses` | 播放解析器 | P0 |
| `headers` | 请求头规则 | P0 |
| `flags` | 需要特殊解析的平台标识 | P0 |
| `proxy` | 网络代理 | P1 |
| `rules` | 媒体识别或嗅探规则 | P1 |
| `hosts` | 域名映射 | P1 |
| `ads` | 广告域名过滤 | P2 |
| `lives` | 直播配置 | 暂不实现 |

### 5.1.4 配置状态

配置列表应展示：

- 配置名称；
- 配置地址或本地路径；
- 当前是否启用；
- 站点数量；
- 可兼容站点数量；
- 最后更新时间；
- 最近一次错误。

---

## 5.2 播放源管理

### 5.2.1 站点列表

系统应解析并展示配置中的 `sites`，主要字段包括：

- `key`；
- `name`；
- `type`；
- `api`；
- `ext`；
- `jar`；
- `searchable`；
- `quickSearch`；
- `changeable`；
- `timeout`；
- `header`；
- `categories`。

### 5.2.2 站点类型

第一阶段应支持：

| 类型 | 调用方式 | 支持要求 |
|---:|---|---|
| 0 | HTTP 接口，XML 返回 | 完整支持 |
| 1 | HTTP 接口，JSON 返回 | 完整支持 |
| 3-JS | JavaScript Spider | 重点支持 |
| 3-Python | Python Spider | 后续支持 |
| 3-JAR | Android Dex/JAR Spider | 第一阶段不支持 |
| 4 | HTTP JSON，扩展参数 Base64 传递 | 完整支持 |

### 5.2.3 站点状态

每个站点应展示：

- 已启用/已停用；
- 是否可搜索；
- 是否支持快速搜索；
- 是否允许线路切换；
- 兼容状态；
- 最近调用结果；
- 最近错误；
- 最近成功时间。

### 5.2.4 站点测试

系统应支持对单个站点执行：

- 首页接口测试；
- 搜索接口测试；
- 详情接口测试；
- 播放接口测试；
- 网络连通性测试。

---

## 5.3 首页与分类

### 5.3.1 默认站点

用户可选择一个站点作为默认首页站点。

### 5.3.2 首页内容

系统应支持调用：

- `homeContent(filter)`；
- `homeVideoContent()`。

首页至少展示：

- 分类入口；
- 推荐影片；
- 最新更新影片。

### 5.3.3 分类浏览

系统应支持：

- 根据分类 ID 获取影片；
- 分类翻页；
- 分类筛选参数；
- 年份、地区、类型等筛选条件；
- 仅展示配置允许的分类。

第一版可将首页和分类作为 P1，在搜索与播放闭环稳定后补齐。

---

## 5.4 视频搜索

### 5.4.1 单站点搜索

系统应支持：

- 指定站点搜索；
- 关键词搜索；
- 页码参数；
- 快速搜索参数；
- 搜索超时；
- 搜索取消。

### 5.4.2 多站点搜索

系统应支持：

- 对所有已启用、可搜索站点并行发起搜索；
- 各站点结果增量返回；
- 单站点失败不影响其他站点；
- 用户发起新搜索时取消旧搜索；
- 显示各站点搜索状态；
- 显示失败和超时站点数量。

### 5.4.3 搜索结果

每个搜索结果至少展示：

- 影片名称；
- 海报；
- 更新说明；
- 年份；
- 来源站点；
- 影片 ID。

第一版不要求对不同站点的同名影片进行合并去重。

### 5.4.4 空状态和异常

应明确区分：

- 未找到结果；
- 站点不支持搜索；
- 搜索超时；
- 插件加载失败；
- 接口返回格式错误；
- 网络错误。

---

## 5.5 影片详情

### 5.5.1 详情内容

系统应调用 `detailContent` 获取并展示：

- 影片名称；
- 海报；
- 类型；
- 年份；
- 地区；
- 导演；
- 演员；
- 简介；
- 更新说明；
- 播放线路；
- 剧集列表。

### 5.5.2 线路解析

系统应兼容：

```text
vod_play_from = "线路一$$$线路二"
```

并将其拆分为多个线路对象。

### 5.5.3 剧集解析

系统应兼容：

```text
第01集$url1#第02集$url2#第03集$url3
```

其中：

- `#` 分隔剧集；
- 第一个 `$` 分隔名称和地址；
- `$$$` 分隔不同线路。

### 5.5.4 详情操作

用户可：

- 选择线路；
- 选择剧集；
- 播放；
- 收藏或取消收藏；
- 查看上次播放位置；
- 从上次进度继续播放。

---

## 5.6 播放解析

### 5.6.1 播放调用链

系统应按以下流程处理：

```text
选择剧集
    ↓
调用站点 playerContent
    ↓
获得 PlayerResult
    ↓
判断是否需要解析
    ├── 不需要：直接播放
    └── 需要：调用解析器
              ↓
          获得真实 URL
              ↓
            播放
```

### 5.6.2 播放结果模型

至少应支持以下字段：

- `url`；
- `parse`；
- `flag`；
- `header`；
- `playUrl`；
- `format`；
- `subs`；
- `drm`；
- `click`；
- `msg`。

### 5.6.3 直接媒体地址

第一阶段必须支持：

- HTTP/HTTPS MP4；
- HLS/M3U8；
- 常见 DASH 流；
- 带查询参数的临时媒体 URL。

### 5.6.4 JSON 解析器

系统应支持配置中的 `parses.type=1`：

```text
解析器地址 + 原始播放地址
    ↓
HTTP 请求
    ↓
读取响应中的 url 或 data.url
    ↓
播放
```

### 5.6.5 URL 前缀

系统应支持 `playUrl + url` 的最终地址拼接逻辑。

### 5.6.6 请求头

播放和解析过程中必须传递：

- User-Agent；
- Referer；
- Cookie；
- Origin；
- 站点自定义 Header。

### 5.6.7 网页嗅探

P1 阶段支持通过内嵌浏览器或独立 WebView 加载网页并识别媒体请求。

应支持：

- 媒体 URL 正则识别；
- 排除规则；
- 自定义点击脚本或点击规则；
- 超时退出；
- 识别成功后终止页面加载。

### 5.6.8 解析失败

解析失败时应提供：

- 失败类型；
- 当前站点；
- 当前线路；
- 当前解析器；
- 原始地址；
- 是否允许手动切换线路或解析器。

---

## 5.7 播放器

### 5.7.1 基础播放

播放器应支持：

- 播放；
- 暂停；
- 停止；
- 拖动进度；
- 音量；
- 静音；
- 全屏；
- 倍速；
- 上一集；
- 下一集；
- 自动播放下一集；
- 播放失败提示。

### 5.7.2 续播

播放器应定期保存：

- 当前播放进度；
- 总时长；
- 当前线路；
- 当前剧集；
- 更新时间。

### 5.7.3 字幕

P1 阶段支持：

- 外挂字幕；
- 远程字幕 URL；
- 字幕轨道切换；
- 字幕延迟调整。

### 5.7.4 播放请求头

播放器必须支持将解析得到的请求头传入媒体请求，避免受保护资源出现 403。

### 5.7.5 播放窗口

应支持：

- 应用内嵌播放；
- 独立播放器页面；
- 进入和退出全屏；
- 键盘快捷键。

---

## 5.8 用户数据管理

### 5.8.1 历史记录

历史记录应保存：

- 站点 Key；
- 影片 ID；
- 影片名称；
- 海报；
- 当前线路；
- 当前剧集；
- 剧集地址；
- 播放位置；
- 总时长；
- 更新时间。

用户可：

- 从历史记录继续播放；
- 删除单条记录；
- 清空历史；
- 按时间排序。

### 5.8.2 收藏

用户可：

- 收藏影片；
- 取消收藏；
- 从收藏进入详情页；
- 删除失效收藏。

### 5.8.3 设置

第一版至少包括：

- 默认配置；
- 默认首页站点；
- 默认播放器内核；
- 默认倍速；
- 是否自动播放下一集；
- 是否记录历史；
- 网络超时；
- 缓存清理。

---

## 6. 页面需求

第一版建议包含六个主要页面。

### 6.1 首页

- 当前配置和默认站点；
- 站点分类；
- 推荐内容；
- 最近观看；
- 搜索入口。

### 6.2 搜索页

- 搜索输入框；
- 全部站点/指定站点切换；
- 搜索进度；
- 结果列表；
- 站点来源标识；
- 失败站点提示。

### 6.3 详情页

- 海报和基本信息；
- 简介；
- 线路选择；
- 剧集列表；
- 收藏；
- 上次观看信息。

### 6.4 播放页

- 视频区域；
- 播放控制栏；
- 线路选择；
- 剧集列表；
- 上一集/下一集；
- 播放设置。

### 6.5 历史与收藏页

- 最近观看；
- 继续播放；
- 收藏列表；
- 删除和清空操作。

### 6.6 配置与播放源页

- 配置添加、删除、刷新和切换；
- 站点列表；
- 兼容状态；
- 启用/停用；
- 站点测试；
- 最近错误。

---

## 7. 非功能需求

### 7.1 性能

- 应用冷启动目标不超过 5 秒；
- 单站点普通搜索超时时间可配置，默认建议 15 秒；
- 多源搜索不得因单个站点阻塞整体界面；
- 搜索结果应增量展示；
- 配置和插件应本地缓存；
- 播放器启动后应尽快进入缓冲状态。

### 7.2 稳定性

- 单个站点崩溃不得导致整个应用退出；
- JavaScript 插件应运行在隔离线程或独立进程；
- 搜索、解析和播放任务应支持取消；
- 插件调用应设置超时；
- 配置更新失败时保留旧版本。

### 7.3 安全性

- 外部插件不得直接获得不受限制的系统文件访问权限；
- 插件网络访问应通过统一网络层；
- 日志中不得默认记录 Cookie、Token 等敏感信息；
- 本地配置和历史数据仅存储在应用数据目录；
- 支持清理缓存、Cookie 和历史记录。

### 7.4 兼容性

第一版建议支持：

- macOS 13 及以上；
- Apple Silicon；
- Intel Mac 作为兼容目标，但可在后续验证；
- 常见 MP4、HLS/M3U8、DASH 视频流。

### 7.5 可维护性

- 核心引擎与 UI 分离；
- 播放源适配器统一接口；
- 播放解析器独立模块；
- 支持以测试用例验证配置、搜索、详情和播放链路；
- 对每个失败步骤保留结构化日志。

---

## 8. 优先级规划

### 8.1 P0：首个可用版本

- 配置 URL/文件导入；
- 配置缓存和刷新；
- `type=0/1/4` HTTP 站点；
- JavaScript Spider；
- 单源搜索；
- 多源并行搜索；
- 影片详情；
- 线路和剧集拆分；
- `playerContent`；
- 直接媒体 URL；
- JSON 解析器；
- 请求头传递；
- mpv 播放；
- 历史记录；
- 断点续播；
- 基础配置管理界面。

### 8.2 P1：体验完善

- 首页和分类；
- 收藏；
- 搜索取消和状态展示；
- 线路切换；
- 手动更换解析器；
- WebView 嗅探；
- 网络代理；
- 字幕；
- 站点健康检测；
- 配置回滚。

### 8.3 P2：后续兼容

- Python Spider；
- JAR Spider 替代或兼容方案；
- 自动换源；
- 超级解析；
- DRM；
- 直播和 EPG；
- 弹幕；
- DLNA；
- 特殊播放协议。

---

## 9. 验收标准

首个可用版本至少应通过以下验收：

1. 可以成功导入一份有效 FongMi/TVBox 点播配置。
2. 可以解析并展示配置中的站点和解析器。
3. 至少验证一个 `type=1` HTTP 源可完成搜索、详情和播放。
4. 至少验证一个 JavaScript Spider 源可完成搜索、详情和播放。
5. 多源搜索时，多个站点结果可以独立、增量返回。
6. 搜索过程中发起新搜索，旧搜索结果不会继续污染当前结果。
7. 详情页可正确拆分多个线路和多个剧集。
8. 可播放普通 MP4 和 HLS/M3U8。
9. 可播放需要 Referer 或 User-Agent 的媒体资源。
10. 可通过 JSON 解析器将网页或平台地址转换为真实媒体地址。
11. 退出播放器后可保存进度，再次进入可继续播放。
12. 单个站点超时或插件异常不会导致应用崩溃。
13. 不支持的 JAR/Python 站点能够明确标识，而不是静默失败。

---

## 10. 风险与约束

### 10.1 JAR Spider 兼容风险

原项目通过 Android `DexClassLoader` 加载 JAR/Dex 插件，依赖 Android Context 和 CatVod Android 类库，无法直接在 macOS JVM 中运行。第一阶段不承诺兼容。

### 10.2 播放源稳定性

播放源和解析器属于外部资源，可能发生：

- 域名失效；
- 接口协议变化；
- 访问限制；
- 需要 Cookie 或登录；
- 网络地域限制；
- 插件自身缺陷。

系统需要把“应用缺陷”和“播放源失效”区分开来。

### 10.3 Web 嗅探复杂度

部分播放地址需要浏览器加载、执行 JavaScript 和拦截网络请求。不同桌面框架对 WebView 网络拦截能力不同，因此应在 P0 完成后单独验证。

### 10.4 签名与分发

若应用需要正式分发，应考虑：

- macOS 应用签名；
- Apple 公证；
- mpv 动态库签名；
- Node.js 或其他运行时打包；
- 外部插件下载和执行策略。

---

## 11. 版本规划建议

### v0.1：核心链路验证

- 无完整 UI；
- 命令行加载配置；
- 搜索、详情、剧集、解析；
- 输出最终媒体 URL；
- 调用 mpv 播放。

### v0.2：桌面 MVP

- 基础 Mac 界面；
- 搜索、详情、播放器；
- 配置管理；
- 历史和续播。

### v0.3：兼容性增强

- 首页和分类；
- Web 嗅探；
- 代理；
- 字幕；
- 更多 JavaScript Spider 兼容函数。

### v1.0：稳定发布版

- 主要播放源类型稳定；
- 完整异常处理；
- 安装包、签名和升级；
- 自动化测试和兼容性清单。

---

## 12. 待确认事项

后续推进前需要逐步确认：

1. 第一版是否只支持 Apple Silicon，还是同时支持 Intel Mac；
2. 是否采用 Electron 作为桌面框架；
3. JavaScript Spider 的兼容目标范围；
4. 是否必须在首版支持 WebView 嗅探；
5. 是否需要兼容现有配置中的远程 JS、相对路径和加密配置；
6. 是否将首页和分类纳入首版，还是首版只保留搜索入口；
7. 应用是否只供个人本地使用，还是需要正式签名分发。
